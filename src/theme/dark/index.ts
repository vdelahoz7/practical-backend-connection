import { colors } from 'src/theme/dark/colors'
import { breakpoints } from 'src/theme/breakpoints'
import { fontSizes } from 'src/theme/fonts'
import { spaces } from 'src/theme/spaces'

const darkTheme = {
    colors,
    breakpoints,
    fontSizes,
    spaces,
}

export default darkTheme
